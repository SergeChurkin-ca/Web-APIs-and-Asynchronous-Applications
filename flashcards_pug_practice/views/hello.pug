extends layout

block content  
    h2 Welcome, user!
    form(ation='/hello', method='post')
        label Please enter your name
            input(type='text', name='username')
        button(type='sumbit') Submit